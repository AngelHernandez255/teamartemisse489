defaults:
  - data: default
  - training: default
  - paths: local
  - logging: default
  - model: svd
  - eval: default
  - inference: default
  - environment: local
  - _self_

project:
  name: teamartemisse489
  phase: phase_2

hydra:
  run:
    dir: outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}
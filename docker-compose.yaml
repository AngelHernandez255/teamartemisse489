version: '3.8'

services:
  teamartemisse489:
    build:
      context: .
      dockerfile: dockerfiles/Dockerfile
    container_name: teamartemisse489_app
    volumes:
      - ./data:/app/data
      - ./models:/app/models
    environment:
      - PYTHONUNBUFFERED=1
    ports:
      - "8000:8000"
